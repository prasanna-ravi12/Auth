<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Success</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f4f9;
            color: #333;
        }

        /* Container Styles */
        .container {
            max-width: 400px;
            margin: 200px auto;
            padding: 35px 50px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* Heading Styles */
        h2 {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #333;
        }

        /* Success Message Styles */
        .message-success {
            padding: 15px;
            margin: 15px 0;
            background-color: #dff0d8;
            border: 1px solid #d6e9c6;
            border-radius: 5px;
            color: #3c763d;
            font-weight: bold;
        }

        /* Error Message Styles */
        .message-error {
            padding: 15px;
            margin: 15px 0;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            border-radius: 5px;
            color: #721c24;
            font-weight: bold;
        }

        /* Link Styles */
        a {
            color: #5cb85c;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #4cae4c;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2><%= message %></h2>
        <div class="<%= messageType %>">
            <%= actionMessage %> <!-- Displays dynamic action message -->
        </div>
        <a href="/">Go to Login</a>
    </div>
</body>
</html>
